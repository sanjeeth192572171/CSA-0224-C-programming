Day 4
1.Check if a number is either divisible by 3 or 7.
#include <stdio.h>
int main() {
    int num;
    printf("Enter a number: ");
    scanf("%d", &num);
    if (num % 3 == 0 || num % 7 == 0)
    printf("%d is divisible by 3 or 7\n", num);
    else
    printf("%d is not divisible by 3 or 7\n", num);
    return 0;
}
2.Accept gender and age; check voting eligibility using nested ternary.
#include <stdio.h>
int main() {
    char gender;
    int age;
    printf("Enter gender (M/F): ");
    scanf(" %c", &gender);
    printf("Enter age: ");
    scanf("%d", &age);
    if (age >= 18) {
    if (gender == 'M' || gender == 'F') {
    printf("Eligible to vote\n");
    } else {
    printf("Invalid gender\n");
    }
    } else {
    printf("Not eligible to vote\n");
    }
    return 0;
}
3. Show result of mixed operations: int a = 3; int b = --a + a++ - ++a;
#include <stdio.h>
int main() {
    int a = 3;
    int b = --a + a++ - ++a;
    printf("a = %d, b = %d\n", a, b);
    return 0;
}
4. A number is called Strong if the sum of the factorials of its digits is equal to the number. Example: 145 = 1! + 4! + 5!
#include <stdio.h>

int main() {
    int n, i, num, digit, fact, sum, j;
    printf("Enter n: ");
    scanf("%d", &n);

    for (i = 1; i <= n; i++) {
    num = i;
    sum = 0;

    while (num > 0) {
    digit = num % 10;
    fact = 1;

    for (j = 1; j <= digit; j++) {
    fact = fact * j;
    }

    sum = sum + fact;
    num = num / 10;
    }

    if (sum == i) {
    printf("%d is a Strong number\n", i);
    }
    }
    return 0;
}
5.Write a C program to find and print all Strong numbers between 1 and N.
#include <stdio.h>
int main() {
    int n,i,num,digit,fact,sum,j;

    printf("Enter n: ");
    scanf("%d",&n);
    for (i = 1; i <= n; i++) {
    num = i;
    sum = 0;
    while (num > 0) {
    digit = num % 10;   
    fact = 1;
    for (j = 1; j <= digit; j++) {
    fact = fact * j;
    }
    sum = sum + fact;   
    num = num / 10;    
    }
    if (sum == i) {
    printf("%d is a Strong number\n", i);
    }
    }
    return 0;
}
6. Write a program to print the numbers from M to N by skipping K numbers in between?
#include <stdio.h>
int main(){
	int m,n,k,i;
	printf("Enter M: "); 
	scanf("%d",&m); 

	printf("Enter N: "); 
	scanf("%d", &n); 

	printf("Enter K (numbers to skip): "); 
	scanf("%d", &k); 

	for (i = m; i <= n; i += (k + 1)) { 
	printf("%d ", i); 
	} 
	return 0; 
	}
