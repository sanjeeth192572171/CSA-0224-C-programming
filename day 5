Day 5

1.Write a program to print whether the
#include <stdio.h>
int main() {
int n, i,sum=0;
printf("Enter a number: ");
scanf("%d", &n);
for(i=1;i<n;i++){
	if (n%i==0){
	sum+=i;
	}
}
if (sum==n){
	printf("perfect no");}
else{
	printf("not a perfect no");}
return 0;
}

2. Find the number of factors for the given
number.
#include <stdio.h>
int main() {
int n, i,sum=0;
printf("Enter a number: ");
scanf("%d", &n);
printf("the factors of %d are:\n",n);
for(i=1;i<n;i++){
	if (n%i==0){
	printf("%d\n",i);
	}
}
return 0;
}


3.lcm &gcd
#include <stdio.h>
int main(){
	int i,a,b,lcm,gcd;
	printf("enter 2 inputs:\n");
	scanf("%d%d",&a,&b);
	lcm=(a>b)?a:b;
	while(1){
		if ((lcm%a==0)&&(lcm%b==0)){
			printf(" lcm os %d",lcm);
 			break;
		}
		lcm++;
	}
	for(i=1;i<=a&&i<=b;i++){
		if((a%i==0)&&(b%i==0)){
			gcd=i;
			printf("gcd is %d",gcd);
		}
	}
	printf("gcd is %d",gcd);
	return 0;
	
}
4.C Program to Check Prime Number: Uses a for loop and a break statement to exit the   loop early if a factor is found, determining  that the number is not prime.


#include <stdio.h>
int main() {
    int n, i, flag = 0;
    printf("Enter a positive integer: ");
    scanf("%d", &n);
    if (n <= 1) {
        printf("%d is not a prime number.\n", n);
        return 0;
    }
    for (i = 2; i <= n / 2; i++) {
        if (n % i == 0) {
            flag = 1;   
            break;}
    }
    if (flag == 0)
        printf("%d is a prime number.\n", n);
    else
        printf("%d is not a prime number.\n", n);
    return 0;
}
5.Write a program to print all the composite
numbers between a and b?
#include <stdio.h>
int main() {
    int a, b, i, j, flag;
    printf("Enter two positive integers (intervals): ");
    scanf("%d %d", &a, &b);
    printf("Composite numbers between %d and %d are:\n", a, b);
    for (i = a; i <= b; i++) {
        if (i <= 1) 
            continue;  
        flag = 0;
        for (j = 2; j <= i / 2; j++) {
            if (i % j == 0) {
                flag = 1;
                break;}
        }
        if (flag == 1)   // composite condition
            printf("%d ", i);
    }
    printf("\n");
    return 0;
}
7.Write a C program to find the LCM of
given two number.
#include <stdio.h>
int main() {
    int num1, num2, max;
    printf("Enter two positive integers: ");
    scanf("%d %d", &num1, &num2);
    max = (num1 > num2) ? num1 : num2;

    while (1) {
        if (max % num1 == 0 && max % num2 == 0) {
            printf("LCM of %d and %d is %d\n", num1, num2, max);
            break;}
        max++;
    }
    return 0;
}
8.Write a C program to Find HCF of Two
Numbers
#include <stdio.h>
int main() {
    int num1, num2, i, hcf;
    printf("Enter two positive integers: ");
    scanf("%d %d", &num1, &num2);
    for (i = 1; i <= num1 && i <= num2; i++) {
        if (num1 % i == 0 && num2 % i == 0) {
            hcf = i;}
    }
    printf("HCF of %d and %d is %d\n", num1, num2, hcf);
    return 0;
}
9.Write a C program to find the given
number is Prime no or not.

#include <stdio.h>
int main() {
    int n, i, flag = 0;
    printf("Enter a positive integer: ");
    scanf("%d", &n);
    if (n <= 1) {
        printf("%d is not a prime number.\n", n);
    } else {
        for (i = 2; i <= n / 2; i++) {
            if (n % i == 0) {
                flag = 1;
                break;}
        }
        if (flag == 0)
            printf("%d is a prime number.\n", n);
        else
            printf("%d is not a prime number.\n", n);
    }
    return 0;
}

10. Sum of Positive & Negative Numbers Separately: Write a program to calculate the
total sum of positive elements and the total sum of negative elements in a list.
Sample Input: -3, 4, -2, 7, 5
Sample Output: Sum Positives: 16, Sum Negatives: -5

#include <stdio.h>
int main() {
    int n, i, num;
    int sumPos = 0, sumNeg = 0;
    printf("Enter how many numbers: ");
    scanf("%d", &n);
    printf("Enter %d integers:\n", n);
    for (i = 0; i < n; i++) {
        scanf("%d", &num);
        if (num > 0)
            sumPos += num;
        else if (num < 0)
            sumNeg += num;
    }
    printf("Sum of Positive Numbers: %d\n", sumPos);
    printf("Sum of Negative Numbers: %d\n", sumNeg);
    return 0;
}
